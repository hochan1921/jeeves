`data/tasks.md` が唯一のダッシュボード。終わったら行を消して完了ログへ1行。
勉強の中身は `data/study/<科目>.md`、1科目1ファイル。書式は見出し・段落・箇条書き・表・リンク・太字まで。
ユーザーの生成物は `data/` の下だけ。フレーム側に書き込まない。
画面（`index.html` / `study.html`）は台帳を持たない。編集はその場で `data/*.md` へ書き戻す。写しを抱えさせない。
コードはむやみに増やさず、安易に機能を追加しない。

人格・口調・振る舞いは @PERSONA.md に従う。ここには書かない。
