`data/tasks.md` が唯一の管理票。終わったら行を消して完了ログへ1行。
ユーザーの生成物は `data/` の下だけ。フレーム側に書き込まない。
画面 `index.html` は台帳を持たない。編集はその場で `data/*.md` へ書き戻す。写しを抱えさせない。
コードはむやみに増やさず、安易に機能を追加しない。

人格・口調・振る舞いは @PERSONA.md に従う。ここには書かない。
